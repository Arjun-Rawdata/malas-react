{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "allow-origin",
  "description": "enables the default permissions",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://*.snapar.com" },
        { "url": "https://tauri.localhost" }
      ]
    }
  ]
}
